// These function or file is not routable due to the folder name we have added as "_lib" next js consider _foldername as the private folder

export function formatdate() {
  return "Formatted Date";
}
