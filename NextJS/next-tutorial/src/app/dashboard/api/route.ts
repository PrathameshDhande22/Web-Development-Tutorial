// Creating the First API Routes for the Next JS
// Hit the Url /dashboard/api
// Create the Function name with the HTTP Verbs only 
export async function GET() {
  return new Response("DashBoard API");
}
