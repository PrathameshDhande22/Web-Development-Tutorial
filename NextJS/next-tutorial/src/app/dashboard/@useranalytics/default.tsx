// For Fallback Render when subnativation for the parallel routes
const UserAnalyticsDefaultPage = () => {
  return <div>User Analytics Default Page</div>;
};

export default UserAnalyticsDefaultPage;
