const Footer = () => {
  return (
    // the jsx must contain one parent element
    <h2>This is the footer</h2>
  )
};

const Body = () => {
  return <h2>This is Body of the App</h2>;
};

export { Footer, Body };
// exporting module as a object
