// ? Using the os Module of Node js

// # Importing the OS module
const os = require("os");

// # Using the methods inside the os module
console.log(os.homedir())
console.log(os.platform())
console.log(os.release())
console.log(os.machine())