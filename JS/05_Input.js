const prompt = require("prompt-sync")()
let age = prompt("Enter your Age : ") //prompts the user and note it will return the prompt as a string
console.log("Your age is ", age)

/* To ask from user input you need to install the module that is prompt-sync using 
npm install prompt-sync and use the line no 1 in every input.
*/